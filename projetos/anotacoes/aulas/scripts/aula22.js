/*Criando métodos
- Os métodos são ações dos objetos, sem limite, podendo resgatar propriedades ou modificar as mesmas.*/
let pessoa = {
    nome: "Wagner",
    idade: 29,
    falar() {
        console.log("Oi")
    }
}

console.log(pessoa)

pessoa.falar()